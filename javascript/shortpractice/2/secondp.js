const number1 = Number(prompt("Enter a number:"))
const number2 = Number(prompt("Enter a number:"))
const number3 = Number(prompt("Enter a number:"))

const maxNumber = Math.max(number1, number2, number3)

alert("Largest number was " + maxNumber)